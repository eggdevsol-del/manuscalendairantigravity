import Subscriptions from "@/pages/Subscriptions";
/** Legacy entry point shares the canonical three-plan screen. */
export function SubscriptionSettings(_props: { onBack: () => void }) {
  return <Subscriptions />;
}
